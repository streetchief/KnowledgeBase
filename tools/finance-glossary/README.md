# Finance Glossary

Needs a `glossary.json` added.

```json
[{
    "word": "Some word",
    "definition": "The definition for the word.",
    "tagString": "Comma separated list of tags"
}]
```

Run with `npx serve ./someDir`
